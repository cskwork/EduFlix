# 검증 및 공개 배포

2026-09-12 사용자가 남은 검증과 문제 수정을 완료하고, 배포를 막는 문제가 없으면 공개 서버에 배포하도록 승인했습니다. 기존 뷰어 권한 유지, 교사 제작과 파일 배포 중심이라는 앞선 결정은 유지합니다.

## 검증 범위와 담당

| 대상 | 담당 | 완료 조건 |
| --- | --- | --- |
| 기존 콘텐츠 84개 | 콘텐츠 검증 에이전트 두 명 | 자료별 시작, 활동, 문항, 완료 또는 반복 흐름의 브라우저 증거를 기록합니다. 직접 장면 주입은 자연스러운 완료로 세지 않습니다. 모바일은 상호작용 유형별로 검증합니다. |
| 작업실과 생성 API | 제품 구현 에이전트 | 저장 손실, 취소·오류, 데이터와 채점 계약, 서버리스 호환성을 검토하고 발견한 결함을 회귀 검증합니다. |
| 배포 대상과 파일 일치 | 코디네이터 | 기존 운영 배포와 설정을 확인하고, 비밀정보가 배포 파일에 포함되지 않도록 확인합니다. 운영 환경 설정으로 배포한 후보를 검증한 후 공개 주소에 연결합니다. |
| 최종 검사 | 코디네이터 | 변경 후 빌드, lint, TypeScript, Vitest, Bun, 콘텐츠 회귀 검사를 실행합니다. |
| 운영 확인 | 코디네이터 | 배포 상태와 주소 연결, 파일 해시, 캐시 헤더, 실제 제작·저장·배포·학습 흐름을 확인합니다. 실제 AI 요청과 모의 오류 검증은 구분합니다. |

## 배포 전 확인

- 작업 경로는 `/Users/danny/Documents/PARA/Resource/EduFlix`이며 기존 변경은 이전 제작 작업의 결과입니다.
- 로컬 `dev`의 기준 커밋은 `bbe09c75c3bb8ecb78c1f7adcd98d0acadb66df2`입니다.
- 2026-09-12 조회한 원격 `dev`는 같은 커밋이며, 원격 `main`은 `065b828e573f53cb65bb08d05fe4110fef78e84d`입니다.
- Vercel 프로젝트는 `agentic-era/eduflix`, 프로젝트 ID는 `prj_nqNE1V4ILImSwxUvAoQAWnIBQSNU`입니다.
- 배포 전 공개 주소 `https://eduflix.vercel.app`는 `dpl_Bz7cX1qu7b7NEcg3Sts9znvxPtEp`에 연결되어 있습니다. 배포 생성 시각은 2026-09-08 23:40:49 KST이며 상태는 Ready였습니다.
- 운영에는 기존 Z.ai 키, 모델, 생성 허용 및 관리자 설정이 있습니다. 값을 출력하거나 문서에 저장하지 않습니다.
- 로컬 Studio는 HTTP 200입니다. `/api/config`는 이 제품의 API가 아니므로 해당 404를 제품 장애로 판단하지 않습니다.

배포 후보는 [Vercel의 단계적 운영 배포 방식](https://vercel.com/docs/cli/deploying-from-cli)에 따라 `--prod --skip-domain`으로 준비합니다. 검증 완료 후 `vercel promote`로 공개 주소를 연결합니다. 새 계정이나 유료 요금제, 인증 정책 변경은 포함하지 않습니다.

## 사용자 요청에 따른 작업 종료

2026-09-12 사용자가 `wrap up`, 이어서 `현재까지 개선만 commit push`를 요청했습니다. 추가 검증과 공개 배포를 중단하고 현재까지의 개선을 `dev`에 저장하는 것으로 작업 범위를 좁혔습니다. 단계적 Vercel 배포나 공개 주소 전환은 실행하지 않았습니다.

브라우저 검증 결과는 다음과 같습니다. 이전의 84개 시작 화면 점검과 이번 전체 흐름 검증을 혼동하지 않습니다.

- 40개는 활동, 문항, 완료 화면을 확인했습니다. 이 중 39개는 재시작도 확인했습니다. `space-diagonal`은 완료 후 화면 캡처가 시간 초과되어 재시작 증거가 없습니다.
- 연속 연습형 6개는 69개 문제 사이클과 재시작을 확인했습니다. 끝 화면이 없는 자료이므로 모든 문항 완주로 세지 않습니다.
- 2개는 부분 검증입니다. `3d-vocabulary-adventure`는 쉬운 난이도만 완료했고, `present-tenses`는 활동 6개와 문항 4개까지 확인했으나 완료·재시작은 확인하지 않았습니다.
- 나머지 36개는 이번 전체 흐름 검증을 완료하지 못했습니다. Phaser 9개와 Mario, 좌표 탐색의 도구 차단 사례, 추가 모바일 터치 회귀가 포함됩니다.

관찰된 진행·채점 문제를 수정했습니다. 긴 장면의 다음 버튼 잘림, 필요한 비교 횟수 안내 누락, `can't` 인라인 이벤트 구문 오류, 섞인 보기의 해설 불일치, 활동 다음 버튼 누락, 터치에서 쓸 수 있는 문장 조립 대체 입력이 해당합니다. 자료별 수정과 한계는 각 담당의 인계 문서에 있습니다.

- [담당 47개 최종 기록](2026-09-12-release-evidence/content/LESSON-CONTENT-HANDOFF.md)
- [Chrome 담당 최종 기록](2026-09-12-release-evidence/contentapp/HANDOFF.md)
- [제품 수정·검토](2026-09-12-release-evidence/product-review.md)
- [작업실 브라우저 회귀](2026-09-12-release-evidence/studio-browser-regression.md)
- [의존성 갱신·감사](2026-09-12-release-evidence/dependencies.md)

미확인 소스 우려도 남겼습니다. `action-verbs-game`의 `width` 참조와 `3d-coordinates` 자유 탐색 장면의 키보드 조건은 실제 브라우저에서 확인·수정하지 않았습니다. 미국 의회도서관 참고 링크는 Cloudflare 보안 확인 화면으로 인해 본문 접근을 검증하지 못했습니다. 수업 실행에는 이 외부 링크가 필요하지 않습니다.

## 커밋 전 검사

- Vitest 4.1.11: 39개 파일, 401개 검사 통과.
- Bun: 명시적으로 선택한 17개 파일, 93개 검사와 840개 단언 통과. 마지막 단독 실행은 4.28초였습니다.
- 콘텐츠 회귀: 기존 40개 실행 그룹과 카탈로그 84개의 JS 59개, 실행 가능한 인라인 스크립트 25개, 이벤트 처리기 366개 구문 검사 통과.
- production build, ESLint, 서버 TypeScript 및 `git diff --check` 통과.
- 운영·전체 npm audit 모두 0건입니다. 직접 의존성 범위 안에서 패치했고 npm/Bun 잠금 파일을 동기화했습니다.
- 배포 dry-run에서 환경 키 파일·DB·로컬 에이전트 상태가 제외됨을 확인했습니다. 실제 업로드나 배포는 하지 않았습니다.

처음 Bun 명령은 파일 경로 대신 광범위한 필터를 사용해 Vitest 전용 파일까지 선택했습니다. 올바른 파일 목록으로 다시 실행했습니다. 동시 검사 중에는 기본 5초 제한을 넘긴 3개 검사가 있어, 동시 작업 종료 후 30초 제한으로 단독 재실행했고 모두 통과했습니다. 이전 실패 기록은 최종 통과 기록과 별도 파일로 보존했습니다.

전체 기능이나 공개 운영 검증 완료를 주장하지 않습니다. 다시 배포 작업을 진행할 때는 미완료 브라우저 항목, 실제 Vercel revision 경로·캐시, 기존 캐시가 있는 브라우저의 새 앱 진입, 실제 운영 환경의 AI 생성과 파일 왕복을 확인해야 합니다. 기존 뷰어 권한은 사용자의 유지 결정을 그대로 따랐습니다.
